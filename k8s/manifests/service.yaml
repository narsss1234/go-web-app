apiVersion: v1
kind: Service
metadata:
  name: go-webapp
spec:
  selector:
    app: go-webapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    app: go-webapp
  type: ClusterIP